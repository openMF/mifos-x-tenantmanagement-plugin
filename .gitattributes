# Windows scripts keep CRLF line endings in the working tree.
*.cmd text eol=crlf
*.bat text eol=crlf

# Shell scripts must stay LF, or they fail to run on Unix after a Windows checkout.
mvnw text eol=lf
*.sh text eol=lf
